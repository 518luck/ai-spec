import { HeaderedPageShell } from "@/widgets/page-shell";
import type { JSX } from "react";

// 渲染个人偏好设置页面占位，等待后续业务内容接入。
export function PreferencesPage(): JSX.Element {
  return <HeaderedPageShell title="个人偏好" />;
}
