NEXT_PUBLIC_APP_NAME="AI Spec"

# 应用公开访问基址，用于后台 worker 拼接邮件链接等绝对 URL
BASE_URL="http://localhost:3000"

# Database
DATABASE_URL="postgresql://duoyun:postgres@localhost:5432/ai_spec?schema=public"

# Axiom logging
AXIOM_TOKEN=""
AXIOM_DATASET=""
IS_AXIOM_ENABLED=false

# Redis rate limiting
REDIS_URL="redis://localhost:6379"

# 邮件发送 provider：resend（生产，默认）| mailpit（本地开发捕获）
EMAIL_PROVIDER="resend"
# Resend email（EMAIL_PROVIDER=resend 时需要）
RESEND_API_KEY=""
# 本地 Mailpit SMTP（EMAIL_PROVIDER=mailpit 时生效）
SMTP_HOST="localhost"
SMTP_PORT="1026"

# S3 兼容对象存储（AWS S3 / Cloudflare R2 / MinIO）
# 自定义服务地址（R2 / MinIO 必填，AWS S3 留空）
S3_ENDPOINT=""
# 数据中心区域，默认 auto
S3_REGION="auto"
# 访问密钥 ID
S3_ACCESS_KEY_ID=""
# 访问密钥
S3_SECRET_ACCESS_KEY=""
# 公开存储桶名
S3_PUBLIC_BUCKET=""
# 私有存储桶名
S3_PRIVATE_BUCKET=""
# 公开访问基址，用于拼接文件 URL
S3_PUBLIC_URL=""

# NextAuth
AUTH_SECRET="change-me"

# GitHub OAuth 登录（用户用 GitHub 账号登录网站用）
# 在 GitHub → Settings → Developer settings → OAuth Apps 创建应用获取
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# GitHub API 令牌：配后广场抓取限额 60→5000 次/时
GITHUB_TOKEN=""

# 广场扫描 cron（UTC）；留空走默认每天 04:00，本地测试可设 "*/1 * * * *"
DISCOVER_SCAN_CRON=""